---
name: cli-mcp
description: Inspect the MCP server status, list registered tools and scopes, run tool invocations, and manage MCP audit logs from the CLI.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect the MCP server status, list registered tools and scopes, run tool invocations, and manage MCP audit logs from the CLI.

## Quick install

```bash
npm install -g niyatnaroute   # or: npx niyatnaroute
niyatnaroute --version
```

## Subcommands

### `mcp`

**Example:**

```bash
niyatnaroute mcp
```

### `mcp status`

Show MCP server status

**Flags:**

- `--json`

**Example:**

```bash
niyatnaroute mcp status
```

### `mcp restart`

Restart the MCP server

**Example:**

```bash
niyatnaroute mcp restart
```

### `mcp call <tool> [argsJson]`

**Flags:**

- `--args <json>`
- `--args-file <path>`
- `--stream`
- `--scope <s>`

**Example:**

```bash
niyatnaroute mcp call <tool> [argsJson]
```

### `mcp scopes`

**Flags:**

- `--tool <name>`

**Example:**

```bash
niyatnaroute mcp scopes
```

### `mcp tools`

**Example:**

```bash
niyatnaroute mcp tools
```

### `mcp list`

**Flags:**

- `--scope <s>`

**Example:**

```bash
niyatnaroute mcp list
```

### `mcp info <name>`

**Example:**

```bash
niyatnaroute mcp info <name>
```

### `mcp schema <name>`

**Flags:**

- `--io <kind>`

**Example:**

```bash
niyatnaroute mcp schema <name>
```

### `mcp audit`

**Example:**

```bash
niyatnaroute mcp audit
```

### `mcp tail`

**Flags:**

- `--follow`
- `--limit <n>`

**Example:**

```bash
niyatnaroute mcp tail
```

### `mcp stats`

**Flags:**

- `--period <p>`

**Example:**

```bash
niyatnaroute mcp stats
```
