---
name: cli-contexts
description: Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.

## Quick install

```bash
npm install -g niyatnaroute   # or: npx niyatnaroute
niyatnaroute --version
```

## Subcommands

### `context-eng`

**Example:**

```bash
niyatnaroute context-eng
```

### `context-eng analytics`

**Flags:**

- `--period <p>`

**Example:**

```bash
niyatnaroute context-eng analytics
```

### `context-eng caveman`

**Example:**

```bash
niyatnaroute context-eng caveman
```

### `context-eng config`

**Example:**

```bash
niyatnaroute context-eng config
```

### `context-eng show`

**Example:**

```bash
niyatnaroute context-eng show
```

### `context-eng set`

**Flags:**

- `--aggressiveness <n>`
- `--max-shrink-pct <n>`
- `--preserve-tags <list>`

**Example:**

```bash
niyatnaroute context-eng set
```

### `context-eng rtk`

**Example:**

```bash
niyatnaroute context-eng rtk
```

### `context-eng config`

**Example:**

```bash
niyatnaroute context-eng config
```

### `context-eng show`

**Example:**

```bash
niyatnaroute context-eng show
```

### `context-eng set`

**Flags:**

- `--token-budget <n>`
- `--reserve-pct <n>`

**Example:**

```bash
niyatnaroute context-eng set
```

### `context-eng filters`

**Example:**

```bash
niyatnaroute context-eng filters
```

### `context-eng list`

**Example:**

```bash
niyatnaroute context-eng list
```

### `context-eng add`

**Flags:**

- `--priority <n>`
- `--action <a>`

**Example:**

```bash
niyatnaroute context-eng add
```

### `context-eng remove <id>`

**Flags:**

- `--yes`

**Example:**

```bash
niyatnaroute context-eng remove <id>
```

### `context-eng test`

**Example:**

```bash
niyatnaroute context-eng test
```

### `context-eng raw-output <id>`

**Example:**

```bash
niyatnaroute context-eng raw-output <id>
```

### `context-eng combos`

**Example:**

```bash
niyatnaroute context-eng combos
```

### `context-eng list`

**Example:**

```bash
niyatnaroute context-eng list
```

### `context-eng get <id>`

**Example:**

```bash
niyatnaroute context-eng get <id>
```

### `context-eng assignments <id>`

**Example:**

```bash
niyatnaroute context-eng assignments <id>
```

### `contexts`

**Example:**

```bash
niyatnaroute contexts
```

### `contexts list`

List all contexts

**Example:**

```bash
niyatnaroute contexts list
```

### `contexts add <name>`

Add a new context

**Flags:**

- `--api-key <k>`
- `--api-key-stdin`
- `--access-token <t>`
- `--access-token-stdin`
- `--scope <s>`
- `--description <d>`

**Example:**

```bash
niyatnaroute contexts add <name>
```

### `contexts use <name>`

Switch active context

**Example:**

```bash
niyatnaroute contexts use <name>
```

### `contexts current`

Show the active context (server, auth, scope)

**Flags:**

- `--name-only`

**Example:**

```bash
niyatnaroute contexts current
```

### `contexts show <name>`

Show context details

**Example:**

```bash
niyatnaroute contexts show <name>
```

### `contexts remove <name>`

Remove a context

**Flags:**

- `--yes`

**Example:**

```bash
niyatnaroute contexts remove <name>
```

### `contexts rename <old> <new>`

Rename a context

**Example:**

```bash
niyatnaroute contexts rename <old> <new>
```

### `contexts export`

Export contexts to JSON

**Flags:**

- `--out <path>`
- `--no-secrets`

**Example:**

```bash
niyatnaroute contexts export
```

### `contexts import <file>`

Import contexts from a JSON file

**Flags:**

- `--merge`

**Example:**

```bash
niyatnaroute contexts import <file>
```

### `sessions`

**Example:**

```bash
niyatnaroute sessions
```

### `sessions list`

**Flags:**

- `--user <u>`
- `--kind <k>`
- `--active`
- `--limit <n>`

**Example:**

```bash
niyatnaroute sessions list
```

### `sessions show <sessionId>`

**Example:**

```bash
niyatnaroute sessions show <sessionId>
```

### `sessions expire <sessionId>`

**Flags:**

- `--yes`

**Example:**

```bash
niyatnaroute sessions expire <sessionId>
```

### `sessions expire-all`

**Flags:**

- `--yes`

**Example:**

```bash
niyatnaroute sessions expire-all
```

### `sessions current`

**Example:**

```bash
niyatnaroute sessions current
```
