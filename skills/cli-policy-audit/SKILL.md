---
name: cli-policy-audit
description: Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.

## Quick install

```bash
npm install -g niyatnaroute   # or: npx niyatnaroute
niyatnaroute --version
```

## Subcommands

### `audit`

**Example:**

```bash
niyatnaroute audit
```

### `audit tail`

**Flags:**

- `--source <s>`
- `--follow`
- `--limit <n>`

**Example:**

```bash
niyatnaroute audit tail
```

### `audit search <query>`

**Flags:**

- `--source <s>`
- `--since <ts>`
- `--until <ts>`
- `--limit <n>`
- `--actor <id>`
- `--action <a>`

**Example:**

```bash
niyatnaroute audit search <query>
```

### `audit export <file>`

**Flags:**

- `--source <s>`
- `--format <f>`
- `--since <ts>`
- `--until <ts>`

**Example:**

```bash
niyatnaroute audit export <file>
```

### `audit stats`

**Flags:**

- `--source <s>`
- `--period <p>`

**Example:**

```bash
niyatnaroute audit stats
```

### `audit get <id>`

**Flags:**

- `--source <s>`

**Example:**

```bash
niyatnaroute audit get <id>
```

### `logs`

**Flags:**

- `--follow`
- `--filter <level>`
- `--lines <n>`
- `--timeout <ms>`
- `--base-url <url>`
- `--request-id <id>`
- `--api-key <key>`
- `--combo <name>`
- `--status <code>`
- `--duration-min <ms>`
- `--duration-max <ms>`
- `--export <path>`

**Example:**

```bash
niyatnaroute logs
```

### `policy`

**Example:**

```bash
niyatnaroute policy
```

### `policy list`

**Flags:**

- `--kind <k>`
- `--scope <s>`

**Example:**

```bash
niyatnaroute policy list
```

### `policy get <id>`

**Example:**

```bash
niyatnaroute policy get <id>
```

### `policy create`

**Example:**

```bash
niyatnaroute policy create
```

### `policy update <id>`

**Example:**

```bash
niyatnaroute policy update <id>
```

### `policy delete <id>`

**Flags:**

- `--yes`

**Example:**

```bash
niyatnaroute policy delete <id>
```

### `policy evaluate`

**Flags:**

- `--resource <r>`
- `--context <json>`

**Example:**

```bash
niyatnaroute policy evaluate
```

### `policy export <file>`

**Example:**

```bash
niyatnaroute policy export <file>
```

### `policy import <file>`

**Flags:**

- `--overwrite`

**Example:**

```bash
niyatnaroute policy import <file>
```

### `telemetry`

**Example:**

```bash
niyatnaroute telemetry
```

### `telemetry summary`

**Flags:**

- `--period <p>`
- `--compare-to <p>`

**Example:**

```bash
niyatnaroute telemetry summary
```

### `telemetry export`

**Flags:**

- `--out <path>`
- `--period <p>`

**Example:**

```bash
niyatnaroute telemetry export
```
